name: Build and Push Docker Image

on:
  push:
    branches: main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3
    
    - name: Login to Docker Hub
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKERHUB_USERNAME }}
        password: ${{ secrets.DOCKERHUB_TOKEN }}
    
    - name: Build and push
      uses: docker/build-push-action@v5
      with:
        context: .
        file: ./Dockerfile.build
        push: true
        tags: ${{ secrets.DOCKERHUB_USERNAME }}/tuckserver:latest
        cache-from: type=registry,ref=${{ secrets.DOCKERHUB_USERNAME }}/tuckserver:buildcache
        cache-to: type=registry,ref=${{ secrets.DOCKERHUB_USERNAME }}/tuckserver:buildcache,mode=max
        platforms: linux/amd64
